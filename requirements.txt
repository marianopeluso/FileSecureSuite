# File Secure Suite — runtime dependencies
cryptography
PySide6

# Optional: enables the clipboard to self-clear automatically after copying
# decrypted text. Without it, the app still works — copy/paste just has to
# be done manually and won't clear itself.
# pyperclip
